# Dependencies
node_modules/

# Build output
dist/
*.tsbuildinfo
.next/
# Studio UI copied into the CLI package at pack time (regenerate via scripts/bundle-studio.mjs)
packages/cli/studio-dist/

# KawnGraph generated graph (regenerate with `kawn scan`)
.kawn/
# Legacy KawnGraph (formerly Athar) graph dir — kept ignored through the rename
.athar/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Env
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Local agent/editor state (share KawnGraph tooling, keep personal settings local)
.claude/*
!.claude/commands/
!.claude/skills/
!.claude/agents/

# Coverage
coverage/

# Benchmark raw transcripts + results (task definitions under benchmarks/ ARE tracked)
benchmark-results/

# Local-only external-project suites (reference private paths/source — never committed)
benchmarks/local/

# Personal, one-off behavioral-eval output (never committed)
agent-eval-output.txt
