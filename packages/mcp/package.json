{
  "name": "@athar/mcp",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "Athar MCP server — read-only Agent Context Graph access for MCP clients (e.g. Claude Code).",
  "bin": {
    "athar-mcp": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "@athar/core": "workspace:*",
    "@athar/shared": "workspace:*"
  }
}
