{
  "name": "@athar/studio-server",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "Athar Studio server — local, read-only HTTP API over an existing .athar/graph.json.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "@athar/core": "workspace:*",
    "@athar/shared": "workspace:*"
  }
}
