{
  "name": "@kawngraph/studio-server",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "KawnGraph Universe server — local, read-only HTTP API over an existing .kawn/graph.json.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b"
  },
  "dependencies": {
    "@kawngraph/core": "workspace:*",
    "@kawngraph/shared": "workspace:*"
  }
}
